Still building the docker container but added Docker Layer Caching, Adding an environment variables via different methods (bash_env, built in environment variables, config injected environment variables)

